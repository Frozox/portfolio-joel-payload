version: '3'

services:
  # payload:
  #   image: node:26-alpine
  #   ports:
  #     - '3000:3000'
  #   volumes:
  #     - .:/home/node/app
  #     - node_modules:/home/node/app/node_modules
  #   working_dir: /home/node/app/
  #   command: sh -c "npx get-pnpm next-12 && pnpm install && pnpm dev"
  #   depends_on:
  #     # - mongo
  #     - postgres
  #   env_file:
  #     - .env

  # Uncomment the following to use postgres
  postgres:
    restart: always
    image: postgres:latest
    volumes:
      - pgdata:/var/lib/postgresql
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: portfolio-joel-payload

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

volumes:
  pgdata:
  node_modules:
